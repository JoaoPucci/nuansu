// @nuansu/schemas — shared zod schemas for the API contract.
// Phase 1 scaffolding: a placeholder export. Full schemas (TranslationObject,
// TranslationStreamChunk, TranslateRequest, etc.) land in Phase 2.

export const SCHEMAS_PACKAGE_VERSION = "0.1.0" as const;
